use std::collections::HashMap;
use std::sync::{Arc};
use tokio::sync::RwLock;
use crate::device::{DeviceConnectionHandles, DeviceConnections};


pub struct Engine {
    pub handles: DeviceConnectionHandles,
}

impl Engine {
    
}